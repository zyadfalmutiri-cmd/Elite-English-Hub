# قاعدة البيانات — Railway يوفّرها تلقائياً عند إضافة خدمة PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/dbname

# مطلوب — ضع كلمة سرية عشوائية طويلة
SESSION_SECRET=change_this_to_a_long_random_secret_string

# Push Notifications (VAPID) — لتوليد مفاتيح جديدة: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=BN1gyZ-QGXEibvjYF6zUZQE2d8k9jefIaMQr3CeHcmhTbQeiQt74iROsJn0JmuZ5w57Lo3FuukXXcYdQH4jiwUI
VAPID_PRIVATE_KEY=your_vapid_private_key_here
VAPID_EMAIL=mailto:admin@vot-english.app

# يوفّره Railway تلقائياً
PORT=8080

# بيئة الإنتاج
NODE_ENV=production
