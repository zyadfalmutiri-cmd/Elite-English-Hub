# قاعدة البيانات — Railway يوفّرها تلقائياً عند إضافة خدمة PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/dbname

# مطلوب — ضع كلمة سرية عشوائية طويلة
SESSION_SECRET=change_this_to_a_long_random_secret_string

# يوفّره Railway تلقائياً
PORT=8080

# بيئة الإنتاج
NODE_ENV=production
